const { Router } = require("express");

const AttenRouter = Router();

AttenRouter.post('/', async (req, res) => {
    try {
        let {} = req.body
     } catch (er) {
        
    }
})

module.exports = AttenRouter